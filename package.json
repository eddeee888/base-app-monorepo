{
  "name": "base-react-app",
  "version": "1.0.0",
  "description": "Base react app boilerplate",
  "main": "index.js",
  "repository": "git@github.com:eddeee888/base-react-app.git",
  "author": "Eddy Nguyen <eddeee.nguyen@gmail.com>",
  "license": "MIT",
  "scripts": {
    "add-route-codegen": "yarn add -D route-codegen && cd client && yarn add route-codegen && cd ../client-seo && yarn add route-codegen && cd ../server && yarn add route-codegen",
    "route-codegen": "route-codegen && cd client && yarn format:prettier && cd ../client-seo && yarn format:prettier && cd ../server && yarn format:prettier"
  },
  "dependencies": {},
  "devDependencies": {
    "route-codegen": "^0.0.6"
  }
}
