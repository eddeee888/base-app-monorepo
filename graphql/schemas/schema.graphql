# import Mutation.* from "admin.graphql"
# import Mutation.* from "authentication.graphql"
# import Mutation.* from "class.graphql"

type Query {
  user(id: ID!): User
  classCategories: [ClassCategory!]!
}

type ClassCategory {
  id: ID!
  name: String!
}
