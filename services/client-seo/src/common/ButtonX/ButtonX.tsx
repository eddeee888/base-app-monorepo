import { routeConfig, RouteConfigProps } from "~/routes/routeConfig";
import { createButtonX } from "~/common/shared-ui/createButtonX";

const ButtonX = createButtonX<RouteConfigProps>(routeConfig);

export default ButtonX;
